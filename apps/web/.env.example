# Copy to .env.local and fill in.
#
# URL of the Cloudflare Worker AI gateway (workers/ai-gateway).
# When unset, the AI panel's "ask" surface returns a clear configuration
# error instead of attempting a fetch.
VITE_AI_GATEWAY_URL=
