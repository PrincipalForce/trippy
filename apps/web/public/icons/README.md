PWA icons.

The 192/512 PNGs in this folder are 1×1 placeholders so the manifest validates
during M0. Replace with proper rasterized icons (matching `/public/favicon.svg`)
before any public release. Required sizes:

- `icon-192.png` — 192×192, any + maskable
- `icon-512.png` — 512×512, any + maskable
